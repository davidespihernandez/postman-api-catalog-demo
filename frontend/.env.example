# Base URL of the Orders API (self-hosted on AWS, path-routed under one host).
# Defaults to this if unset. The client calls ${BASE}/orders, ${BASE}/orders/{id}.
VITE_ORDERS_API_URL=https://18-157-170-15.nip.io
