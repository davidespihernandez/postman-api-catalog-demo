{
  "name": "orders-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Simple Orders UI for the Postman API Catalog demo — drives the Orders API (self-hosted on AWS) from a browser so Postman can capture the traffic during Playwright tests.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "test": "playwright test"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@vitejs/plugin-react": "^4.3.1",
    "postman-playwright": "^1.1.1",
    "vite": "^5.4.8"
  }
}
