owner: "53522859"
team: "14577123"
name: GCP Payments
color: null
values:
  - key: baseUrl
    # Self-hosted APIs on the GCP VM (observed by the Insights agent), one DuckDNS host,
    # path-routed: the Payments collection calls {{baseUrl}}/payments, etc.
    # Replace CHANGE-ME with your DuckDNS subdomain (see runtime-vm/.env HOSTNAME).
    value: https://CHANGE-ME.duckdns.org
    enabled: true
