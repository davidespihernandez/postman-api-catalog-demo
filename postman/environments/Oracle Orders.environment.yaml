owner: "53522859"
team: "14577123"
name: Oracle Orders
color: null
values:
  - key: baseUrl
    # Self-hosted Orders API on the Oracle VM (observed by the Insights agent).
    # Replace CHANGE-ME.nip.io with your VM hostname (see runtime-vm/.env HOSTNAME).
    value: https://orders.CHANGE-ME.nip.io
    enabled: true
