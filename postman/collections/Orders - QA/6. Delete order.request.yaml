$kind: http-request
url: "{{baseUrl}}/orders/{{qaRecordId}}"
method: DELETE
scripts:
  - type: afterResponse
    code: pm.test('Status 204 No Content', function () { pm.response.to.have.status(204); });
    language: text/javascript
order: 6000
