$kind: http-request
url: "{{baseUrl}}/payments/:id"
method: DELETE
headers:
  Accept: application/json
pathVariables:
  - key: id
    value: pay-001
    description: Payment identifier
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string",
            "example": "pay-001"
          }
        },
        "required": [
          "value"
        ]
      }
examples: ./.resources/Delete payment.resources/examples
order: 4000
