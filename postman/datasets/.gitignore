# Lock files from dataset YAML concurrent writes
**/*.lock

# Temp files for dataset creation
.temp/

# DuckDB engine file (generated from the CSV; the `-d <csv>` run doesn't need it)
**/.resources/data.db
